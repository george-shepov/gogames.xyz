'use strict';
$('#btn-new-game').addEventListener('click',startNewMatch);$('#btn-tutorial').addEventListener('click',tutorialStart);$('#btn-sound').addEventListener('click',()=>{state.sound=!state.sound;const btn=$('#btn-sound');btn.textContent=state.sound?'🔊':'🔇';btn.setAttribute('aria-pressed',String(state.sound));try{localStorage.setItem(SOUND_KEY,state.sound?'on':'off')}catch(_){}if(state.sound)beep(520,.06,.02)});$('#btn-rules').addEventListener('click',()=>openModal('rules-modal'));$('#btn-stats').addEventListener('click',()=>{updateStatsUI();openModal('stats-modal')});$('#btn-reset-stats').addEventListener('click',()=>{stats={matches:0,wins:0,bestScore:0};try{localStorage.setItem(STATS_KEY,JSON.stringify(stats))}catch(_){}updateStatsUI()});$('#btn-confirm-bid').addEventListener('click',confirmBid);$('#btn-suggestion').addEventListener('click',()=>selectBid(shouldBidNil(state.hands[0])?0:estimateBid(state.hands[0])));$('#btn-next-round').addEventListener('click',nextRound);$('#btn-end-match').addEventListener('click',endMatch);$$('[data-close]').forEach(btn=>btn.addEventListener('click',()=>closeModal(btn.dataset.close)));$$('.modal-backdrop').forEach(bd=>bd.addEventListener('click',e=>{if(e.target===bd&&bd.id!=='bid-modal'&&bd.id!=='round-modal')closeModal(bd.id)}));document.addEventListener('keydown',e=>{if(e.key!=='Escape')return;if(tutorial.active){tutorialEnd('Tutorial skipped. Start a new match when ready.',true);return}if(!$('#rules-modal').classList.contains('hidden'))closeModal('rules-modal');if(!$('#stats-modal').classList.contains('hidden'))closeModal('stats-modal')});$('#btn-sound').textContent=state.sound?'🔊':'🔇';$('#btn-sound').setAttribute('aria-pressed',String(state.sound));updateStatsUI();if(restoreGameState()){renderAll();clearSlots();state.trick.forEach(play=>renderPlayedCard(play.player,play.card));if(state.phase==='bidding')startBidding();else if(state.phase==='dealing')dealRound();else if(state.phase==='playing'){$('#center-msg').textContent=state.turn===0?'Your turn.':`${NAMES[state.turn]}'s turn.`;toast('Saved match restored.',true);if(state.trick.length===4)timer=setTimeout(resolveTrick,500);else if(state.turn!==0)timer=setTimeout(()=>aiPlay(state.turn),AI_DELAY)}else if(state.phase==='round-end'&&state.roundSummary){$('#center-msg').textContent=`Round ${state.round} complete.`;toast('Saved match restored.',true);renderRoundResultModal()}}else{renderAll();const tutorialDone=localStorage.getItem(TUTORIAL_KEY)==='done';toast(tutorialDone?'Welcome to FieldKit Spades Royale. Start a new match to begin.':'Welcome! 👋 New to Spades? Click <strong>📖 Tutorial</strong> for a quick walkthrough.',true);$('#center-msg').textContent='Press ✦ New Match or 📖 Tutorial.'}
